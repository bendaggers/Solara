# Solara AI Quant — Dependencies
# Install: pip install -r requirements.txt
#
# REQUIRED PYTHON VERSION: 3.11.7
# (Tested and running on Python 3.11.7. Use pyenv or the official installer to
#  match this exactly on your VPS. Python 3.12+ is NOT verified.)
#
# All versions are pinned to what is running on the dev machine (2026-04-22).
# This ensures the VPS environment matches exactly and avoids compatibility issues.
#
# NOTE: MetaTrader5 is Windows-only. On a Linux VPS SAQ runs in development
# mode (no live orders). For production trading you need a Windows VPS with MT5.

# ── Core ──────────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
PyYAML==6.0.1
pydantic==2.12.5
pydantic_core==2.41.5          # required by pydantic
annotated-types==0.7.0         # required by pydantic
typing_extensions==4.15.0      # required by pydantic + sqlalchemy
typing-inspection==0.4.2       # required by pydantic

# ── MT5 (Windows only) ────────────────────────────────────────────────────────
MetaTrader5==5.0.5735          # omit or comment out on Linux VPS

# ── Data Processing ───────────────────────────────────────────────────────────
pandas==2.2.1
numpy==1.26.4
python-dateutil==2.9.0.post0   # required by pandas
pytz==2024.1                   # required by pandas
tzdata==2026.1                 # required by pandas on Windows

# ── Machine Learning ──────────────────────────────────────────────────────────
scikit-learn==1.5.2
scipy==1.14.1                  # required by scikit-learn
threadpoolctl==3.5.0           # required by scikit-learn
lightgbm==4.6.0
xgboost==2.1.1
catboost==1.2.10
joblib==1.4.2

# ── File Watching ─────────────────────────────────────────────────────────────
watchdog==6.0.0

# ── Database ──────────────────────────────────────────────────────────────────
SQLAlchemy==2.0.46
greenlet==3.3.1                # required by SQLAlchemy async

# ── Logging & Display ─────────────────────────────────────────────────────────
colorama==0.4.6
structlog==25.5.0
rich==14.3.3
markdown-it-py==4.0.0          # required by rich
mdurl==0.1.2                   # required by rich
Pygments==2.17.2               # required by rich

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==9.0.2
pluggy==1.6.0                  # required by pytest
iniconfig==2.3.0               # required by pytest
packaging==23.2                # required by pytest
